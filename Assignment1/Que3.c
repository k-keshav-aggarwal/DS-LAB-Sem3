#include <stdio.h>

int main()
{
    int i;
    int arr[5] = {1};
    for (i = 0; i < 5; i++)
    printf("%d",arr[i]);
    return 0;
}
 
// The output for the above code will be : 10000 // Tested By Dry Running as well as by running the code